name = "debug_generic_tests"
description = "Tests for the built-in Alire test runner"
version = "0.0.0-test"

[[depends-on]]
debug_generic = "*"

[[pins]]
debug_generic = { path = ".." }

[build-profiles]
debug_generic       = "validation"
debug_generic_tests = "validation"
